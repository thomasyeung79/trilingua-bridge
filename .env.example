# Copy to .env and fill in your API key.
OPENAI_API_KEY=your_openai_api_key_here

# Optional: choose a model. Defaults to gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# Optional: choose a DB path. Defaults to trilingua.db in project root.
DB_PATH=trilingua.db
# Core
streamlit>=1.32
python-dotenv>=1.0

# LLM providers
openai>=1.30
anthropic>=0.30

# Audio: TTS, STT, romanization
gTTS>=2.5
langdetect>=1.0.9
pypinyin>=0.50
hangul-romanize>=0.1.0
eng-to-ipa>=0.0.2
pykakasi>=2.2

# Optional but recommended (the app degrades gracefully if these are missing)
streamlit-mic-recorder>=0.0.8   # in-browser microphone capture
pycantonese>=3.4                # Jyutping romanization for Cantonese

# Dev
pytest>=7.0
ruff>=0.4

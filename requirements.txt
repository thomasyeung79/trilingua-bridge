# Core
streamlit>=1.33.0
# python-dotenv is kept for local development (.env file loading).
# On Streamlit Cloud it is harmless — secrets are read via st.secrets instead.
python-dotenv>=1.0.1

# LLM providers
openai>=1.30.0
anthropic>=0.30

# Audio: TTS, STT, romanization
gTTS>=2.5.1
langdetect>=1.0.9
pypinyin>=0.50
hangul-romanize>=0.1.0
eng-to-ipa>=0.0.2
pykakasi>=2.2

# Optional but recommended (the app degrades gracefully if these are missing)
streamlit-mic-recorder>=0.0.8   # in-browser microphone capture
pycantonese>=4.0.0              # Jyutping romanization for Cantonese

# Error monitoring (optional — only needed when SENTRY_DSN is configured)
sentry-sdk>=2.0

# PostgreSQL / Supabase (optional — only needed when USE_POSTGRES=true)
psycopg2-binary>=2.9
